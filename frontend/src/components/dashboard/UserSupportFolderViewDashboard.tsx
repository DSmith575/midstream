import { SupportFolderCard } from '@/components/profile/card/support/SupportFolderCard'

export const SupportFolderView = ({ userId }: { userId: string }) => {
  return <SupportFolderCard userId={userId} />
}